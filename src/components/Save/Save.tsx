import React, { FC } from 'react'
import './Save.css'

const Save: FC = () => {
    return (
        <section className="save">
            <button className="save__btn btn">Add</button>
        </section>
    )
}

export default Save
